# 개발일지 - 2026.03.09 (Day 3~5)

## Day 3~4 (이전 세션)
Day 2에서 페이지↔서비스 연결과 UI 개선을 완료한 후, Day 3~4에서는 **결제 시스템, 검색, 주소검색, 몰 스토어프론트 실데이터, 마이페이지/주문관리/이미지업로드**를 구현. Phase A 전체 완료.

## Day 5 - Phase D: 마켓인사이드 핵심 기능 구현

### 완료 항목 (8개 핵심 기능)

| # | 기능 | 서비스 | 페이지 수 |
|---|------|--------|----------|
| D-1 | 포인트 시스템 | point-service.ts | 2 |
| D-2 | 회원등급 | grade-service.ts | 2 |
| D-3 | 쿠폰 시스템 | coupon-service.ts | 4 |
| D-4 | 배송 관리 | shipping-service.ts | 1 |
| D-5 | 엑셀 대량처리 | excel-service.ts | 1 |
| D-6 | 공급사 포탈 (B2B) | supplier-service.ts | 6 |
| D-7 | SMS/알림 | notification-service.ts | 3 |
| D-8 | 세금계산서/정산서 | report-service.ts | 5 |

### 인프라 업데이트
- 타입 30+ 추가 (포인트, 등급, 쿠폰, 배송존, 공급사, 알림, 세금계산서 등)
- Firestore 규칙 10+ 컬렉션 추가
- AdminSidebar 메뉴 확장 (공급사, 쿠폰, 정산, 세금계산서, 알림)
- User/Order/Mall/Supplier/Settlement 인터페이스 확장

### 빌드 결과
- **78개 라우트** 빌드 성공 (기존 49개 → 78개)
- TypeScript 에러 0개

### 해결한 이슈
- Select onChange 타입 (6곳), EmptyState icon (6곳), Table column (3곳)
- Order/User/Mall 타입 확장 후 기존 코드 호환성 수정

## Day 5 추가 - 체크아웃 통합

### 완료 항목
- **체크아웃 페이지 포인트/쿠폰/배송비 통합** (`src/app/checkout/page.tsx`)
  - 포인트: 잔액 표시, 입력/전액사용, 결제 후 자동 차감
  - 쿠폰: 코드 입력 → 실시간 검증 → 할인 적용, 결제 후 사용처리
  - 배송비: 우편번호 기반 배송존별 동적 계산
  - 결제 금액 표시: 상품할인/쿠폰할인/포인트 사용 각각 분리 표시
- 빌드 성공 (78개 라우트, 에러 0개)

## Day 5 추가 - Cloud Functions 확장

### 완료 항목
- **새 스케줄 함수 3개**:
  - `expirePoints` - 매일 03:00 포인트 만료 처리
  - `evaluateGrades` - 매일 04:00 회원등급 재평가
  - `expireCoupons` - 매일 01:00 쿠폰 만료 처리
- **새 트리거 함수 1개**:
  - `onSettlementComplete` - 정산 완료 시 정산서 + 세금계산서 자동 생성
- **기존 함수 확장** (`onOrderStatusChange`):
  - 배송완료 시 포인트 자동 적립 (`handlePointEarning`)
  - 주문 취소 시 포인트/쿠폰 복구 (`handleRestorePointsAndCoupons`)
- Functions 빌드 성공 (TypeScript 에러 0개)
- Next.js 빌드 성공 (78개 라우트, 에러 0개)

## Day 5 추가 - Phase C: SEO/품질

### 완료 항목
- **JSON-LD 구조화 데이터** (`src/app/page.tsx`) - Schema.org WebSite + SearchAction
- **sitemap.xml** (`src/app/sitemap.ts`) - 11개 정적 페이지, `force-static` 설정
- **robots.txt** (`src/app/robots.ts`) - admin/api/mypage 등 크롤링 차단
- **메타데이터 확인** - 전 레이아웃/페이지에 metadata export 존재 확인
- 빌드 성공 (80개 라우트: 78 + sitemap.xml + robots.txt)

### 해결한 이슈
- `output: 'export'` 모드에서 sitemap/robots 빌드 에러 → `export const dynamic = 'force-static'` + `process.env` 제거

## Day 5 추가 - Phase B: 인프라/배포 준비

### 완료 항목
- **`.env.local.example`** 생성 - 환경변수 템플릿 (Firebase, PortOne, SMS, 세금계산서)
- **Firestore 복합 인덱스** 37개 추가 (`firestore.indexes.json`)
  - points_ledger (5개), coupons (4개), suppliers (3개), notification_history (3개)
  - tax_invoices (2개), settlement_reports (1개), supplier_settlements (1개) 등
- **Storage 보안 규칙 보강** (`storage.rules`)
  - 공급사 문서, 정산서/PDF, 엑셀 업로드 경로 추가
- **배포 스크립트** 7개 (`package.json`)
  - `deploy`, `deploy:hosting`, `deploy:functions`, `deploy:rules`, `deploy:indexes`, `functions:build`, `emulators`
- **보안 헤더 강화** (`firebase.json`)
  - `Permissions-Policy`, `Strict-Transport-Security` 추가
- 빌드 성공 (80개 라우트, 에러 0개)

## Day 5 추가 - 프로덕션 배포

### 완료 항목
- **Firebase 전체 배포 완료** (https://marketshare-2e00c.web.app)
  - Hosting: 673개 파일
  - Cloud Functions: 14개 (13 기존 + chatbot 신규)
  - Firestore Rules + Indexes (37개)
  - Storage Rules
- `onUserCreate` 수정: `beforeUserCreated`(GCIP 필요) → `auth.user().onCreate`(v1)

## Day 5 추가 - AI 챗봇 탑재

### 완료 항목
- **Cloud Function** (`functions/src/chat/chatbot.ts`)
  - API 키 있으면: Claude Haiku AI 자연어 상담
  - API 키 없으면: 10개 카테고리 룰 기반 FAQ 자동 응답 (무료)
  - 대화 기록 유지 (최근 10턴), 500자 제한
- **챗봇 위젯** (`src/components/chat/ChatWidget.tsx`)
  - 우하단 플로팅 버튼, 채팅 창 UI
  - 빠른 질문 4개 (배송/무료배송/교환반품/결제수단)
  - 로딩 애니메이션, 자동 스크롤
  - `settings/chatbot` Firestore 문서로 활성화/비활성화 제어
- **관리자 설정** (`src/app/admin/settings/page.tsx`)
  - 챗봇 활성화 토글
  - Anthropic API 키 입력 (선택)
  - 동작 방식 안내 (FAQ vs AI)
- 전체 레이아웃에 ChatWidget 탑재 (`src/app/layout.tsx`)
- 배포 완료 (14개 Cloud Functions)

### 남은 작업
- 외부 API 연동 (SMS, 세금계산서) - API 계정 필요
